import { render, screen } from '@testing-library/react'
import { FilterByPrice } from '.'

describe('<FilterByPrice />', () => {
  it('should render filter', () => {
    //render(<FilterByPrice />);
    //const input = screen.getByLabelText('Até R$40');
  })
})