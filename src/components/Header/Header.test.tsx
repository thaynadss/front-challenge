import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event';
import { Header } from '.'

describe('Header />', () => {
  it('should do something', () => {
    // const fn = jest.fn();
    // render(<Header searchClick={false} cartClick={false} hamburgerClick={false} handleHamburgerClick={fn} handleSearchClick={fn} handleCartClick={fn} />)

    // const menuHamburger = screen.getByAltText('Menu');
    // userEvent.click(menuHamburger);
    // expect(fn).toHaveBeenCalledTimes(1);

  });
});